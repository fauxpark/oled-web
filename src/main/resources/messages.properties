# Initialisation
display.alreadyinitialised=Display is already initialised
display.notinitialised=Display has not been initialised

# Flip
display.flip.invalidaxis=Invalid flip axis specified: {0}

# Contrast
display.contrast.invalidlevel=Invalid contrast level specified: {0}

# Offset
display.offset.invalidoffset=Invalid offset specified: {0}

# Pixel
display.pixel.invalidcoordinates=Invalid pixel coordinates specified: ({0},{1})

# Buffer
display.buffer.sizemismatch=Display buffer size ({0} bytes) must match existing size ({1} bytes)

# Scroll
display.scroll.alreadyactive=Display is already scrolling
display.scroll.notactive=Display is not scrolling

# Font
display.font.invalidfont=Invalid font specified: {0}

# Image
display.image.invaliddimensions=Image must have a width and height of at least 1 pixel

# Rectangle
display.rectangle.invaliddimensions=Rectangle must have a width and height of at least 1 pixel

# Arc
display.arc.invalidradius=Arc must have a non-negative radius

# Circle
display.circle.invalidradius=Circle must have a non-negative radius
